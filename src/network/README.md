# network/

Wire protocol and transport for cluster communication.
Lands in Phase 2 (see SPEC.md §8).

Planned modules:
- `protocol.c` — frame format, message types
- `transport.c` — TCP listener/dialer, connection state
